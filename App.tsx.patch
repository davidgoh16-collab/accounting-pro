<<<<<<< SEARCH
const App: React.FC = () => {
    const [user, setUser] = useState<AuthUser | null>(null);
    const [userYearGroup, setUserYearGroup] = useState<string | null>(null); // Start null to prevent leaking defaults
    const [isLoading, setIsLoading] = useState(true);
=======
const App: React.FC = () => {
    // FORCE USER
    const [user, setUser] = useState<AuthUser | null>({ uid: 'test', displayName: 'Test User', email: 'test@example.com', level: 'A-Level' });
    const [userYearGroup, setUserYearGroup] = useState<string | null>(null); // Start null to prevent leaking defaults
    const [isLoading, setIsLoading] = useState(false);
>>>>>>> REPLACE
<<<<<<< SEARCH
    useEffect(() => {
        const unsubscribe = onAuthChange(async (firebaseUser) => {
            if (firebaseUser) {
=======
    useEffect(() => {
        // Disabled Auth for Verification
        return () => {};
    }, []);
>>>>>>> REPLACE
